# notes-hub

Talk, don't type. Drop a raw idea in chat and Claude Code turns it into a short markdown note in `notes/` using the `capture-idea` skill.

Each note: raw idea → plain-language translation → next actions. See `templates/idea-template.md`.
